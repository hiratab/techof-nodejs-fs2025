import { Router } from "express";
import { Student } from "../models/Student.js";

const router = Router();

router.get("/", async (req, res) => {
  // Implementar paginação
  // Implementar listagem dos estudantes
});

export default router;
