const { Course } = require('./Course');
const { Student } = require('./Student');
const { Post } = require('./Post');

module.exports = {
  CourseModel: Course,
  StudentModel: Student,
  PostModel: Post,
};